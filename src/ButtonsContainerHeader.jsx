import React from "react";

const ButtonsContainerHeader = props => {
  return (
    <div className="buttons-container-header borders">{props.children}</div>
  );
};

export default ButtonsContainerHeader;
